def area_or_perimeter(l , w):
    if l == w:
        return l * w
    else:
        return 2 * (l + w)
    # return your answer
#     If length == width, it's a square, so return the area (length × width).
​
# Otherwise, it's a rectangle, so return the perimeter (2 × (length + width)).
​
​